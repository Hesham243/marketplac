
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Edit Listing</title>
</head>
<body>
  <header>
    <h1>Edit Listing</h1>
  </header>

  <div class="container">
    <div class="listing-detail">
      <form action="/listings/<%= foundListing._id %>?_method=PUT" method="POST" class="listing-form">
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%= foundListing.title %>" required>

        <label for="description">Description:</label>
        <textarea name="description" id="description" rows="4"><%= foundListing.description %></textarea>

        <label for="price">Price:</label>
        <input type="number" name="price" id="price" step="0.01" min="0" value="<%= foundListing.price %>">

        <label for="image">Image URL:</label>
        <input type="text" name="image" id="image" value="<%= foundListing.image %>">

        <button type="submit" class="btn-submit">Update Listing</button>
      </form>
      
    </div>
  </div>
</body>
</html>
